import config from '../config/app.config.js';

class APIService {
    constructor() {
        this.baseURL = config.API_BASE_URL;
        this.apiPrefix = '/api';
        this.defaultHeaders = {
            'Content-Type': 'application/json'
        };
        this.requestTimeout = 30000;
    }

    async request(endpoint, options = {}) {
        const url = `${this.baseURL}${this.apiPrefix}${endpoint}`;
        const maxRetries = options.retries || 3;
        const retryDelay = options.retryDelay || 1000;
        const config = {
            timeout: this.requestTimeout,
            headers: {
                ...this.defaultHeaders,
                ...options.headers
            },
            ...options
        };for(let attempt=0;attempt < maxRetries;attempt++){try{const controller=new AbortController();const timeoutId=setTimeout(()=> controller.abort(),config.timeout);const response=await fetch(url,{...config,signal:controller.signal});clearTimeout(timeoutId);if(!response.ok){if(response.status >=400 && response.status < 500){throw new APIError(`HTTP ${response.status}:${response.statusText}`,response.status,await this.parseErrorResponse(response));}if(attempt===maxRetries-1){throw new APIError(`HTTP ${response.status}:${response.statusText}`,response.status,await this.parseErrorResponse(response));}console.warn(`API request failed(attempt ${attempt+1}/${maxRetries}):${response.status}${response.statusText}`);await this.delay(retryDelay*Math.pow(2,attempt));continue;}return await this.parseResponse(response);}catch(error){if(error.name==='AbortError'){if(attempt===maxRetries-1){throw new APIError('Request timeout',408,{timeout:true});}console.warn(`Request timeout(attempt ${attempt+1}/${maxRetries})`);await this.delay(retryDelay*Math.pow(2,attempt));continue;}if(error instanceof APIError){throw error;}if(attempt===maxRetries-1){throw new APIError('Network error',0,{network:true,original:error});}console.warn(`Network error(attempt ${attempt+1}/${maxRetries}):`,error.message);await this.delay(retryDelay*Math.pow(2,attempt));}}}delay(ms){return new Promise(resolve=> setTimeout(resolve,ms));}async parseResponse(response){const contentType=response.headers.get('content-type');if(contentType && contentType.includes('application/json')){return await response.json();}if(contentType && contentType.includes('text/')){return await response.text();}return await response.blob();}async parseErrorResponse(response){try{const contentType=response.headers.get('content-type');if(contentType && contentType.includes('application/json')){return await response.json();}return{message:await response.text()};}catch{return{message:'Unknown error'};}}async get(endpoint,params={}){const searchParams=new URLSearchParams(params);const queryString=searchParams.toString();const url=queryString ? `${endpoint}?${queryString}`:endpoint;return this.request(url,{method:'GET'});}async post(endpoint,data={}){return this.request(endpoint,{method:'POST',body:JSON.stringify(data)});}async put(endpoint,data={}){return this.request(endpoint,{method:'PUT',body:JSON.stringify(data)});}async patch(endpoint,data={}){return this.request(endpoint,{method:'PATCH',body:JSON.stringify(data)});}async delete(endpoint){return this.request(endpoint,{method:'DELETE'});}async upload(endpoint,file,additionalData={}){const formData=new FormData();formData.append('file',file);Object.entries(additionalData).forEach(([key,value])=>{formData.append(key,value);});return this.request(endpoint,{method:'POST',body:formData,headers:{}});}}class APIError extends Error{constructor(message,status,details={}){super(message);this.name='APIError';this.status=status;this.details=details;}}class CameraService extends APIService{constructor(){super();this.endpoint='/cameras';}async getCameras(filters={}){return this.get(this.endpoint,filters);}async getCamera(id){return this.get(`${this.endpoint}/${id}`);}async createCamera(cameraData){return this.post(this.endpoint,cameraData);}async updateCamera(id,cameraData){return this.put(`${this.endpoint}/${id}`,cameraData);}async deleteCamera(id){return this.delete(`${this.endpoint}/${id}`);}async testConnection(connectionData){return this.post(`${this.endpoint}/test-connection`,connectionData);}async testConnectionEnhanced(connectionData){return this.post(`${this.endpoint}/test-connection-enhanced`,connectionData);}async discoverCameras(discoveryParams={}){return this.post(`${this.endpoint}/discover`,discoveryParams);}async getCameraStreams(cameraId){return this.get(`${this.endpoint}/${cameraId}/streams`);}async getCameraSnapshot(cameraId,streamId='main'){return this.get(`${this.endpoint}/${cameraId}/snapshot`,{stream:streamId});}async openInVLC(cameraId){return this.post(`${this.endpoint}/${cameraId}/open-vlc`);}async startPreview(cameraId,streamId='main'){return this.post(`${this.endpoint}/${cameraId}/preview`,{stream:streamId});}async stopPreview(cameraId,sessionId){return this.delete(`${this.endpoint}/${cameraId}/preview/${sessionId}`);}async getManufacturers(){return this.get(`${this.endpoint}/manufacturers`);}async bulkUpdate(cameraIds,updateData){return this.post(`${this.endpoint}/bulk-update`,{camera_ids:cameraIds,...updateData});}async bulkDelete(cameraIds){return this.delete(`${this.endpoint}/bulk-delete`,{body:JSON.stringify({camera_ids:cameraIds}),headers:{'Content-Type':'application/json'}});}}class DetectionService extends APIService{constructor(){super();this.endpoint='/detections';}async getDetections(filters={}){return this.get(this.endpoint,filters);}async getDetection(id){return this.get(`${this.endpoint}/${id}`);}async updateDetection(id,data){return this.put(`${this.endpoint}/${id}`,data);}async deleteDetection(id){return this.delete(`${this.endpoint}/${id}`);}async getRecentDetections(limit=10,camera_id=null){const params={limit};if(camera_id)params.camera_id=camera_id;return this.get(`${this.endpoint}/recent`,params);}async searchDetections(filters={}){return this.get(`${this.endpoint}/search`,filters);}async exportDetections(filters={},format='csv'){return this.get(`${this.endpoint}/export`,{...filters,format});}async getDetectionStats(timeframe='24h'){return this.get(`${this.endpoint}/stats`,{timeframe});}async flagDetection(id,reason=''){return this.post(`${this.endpoint}/${id}/flag`,{reason});}async verifyDetection(id){return this.post(`${this.endpoint}/${id}/verify`);}async bulkAction(detectionIds,action,data={}){return this.post(`${this.endpoint}/bulk-action`,{detection_ids:detectionIds,action,...data});}}class AnalyticsService extends APIService{constructor(){super();this.endpoint='/analytics';}async getOverview(){return this.get(`${this.endpoint}/overview`);}async getHourlyTrend(hours=24){return this.get(`${this.endpoint}/trend`,{hours});}async getCameraStats(cameraId=null){const params=cameraId ?{camera_id:cameraId}:{};return this.get(`${this.endpoint}/cameras`,params);}async getPlateStats(timeframe='24h'){return this.get(`${this.endpoint}/plates`,{timeframe});}}class SystemService extends APIService{constructor(){super();this.endpoint='/system';}async getHealth(){return this.get('/health');}async getStatus(){return this.get(`${this.endpoint}/status`);}async getMetrics(){return this.get(`${this.endpoint}/metrics`);}async getVersion(){return this.get(`${this.endpoint}/version`);}async getLogs(level='info',limit=100){return this.get(`${this.endpoint}/logs`,{level,limit});}async runDiagnostics(){return this.post(`${this.endpoint}/diagnostics`);}async getConfiguration(){return this.get(`${this.endpoint}/config`);}async updateConfiguration(config){return this.put(`${this.endpoint}/config`,config);}async restartService(service){return this.post(`${this.endpoint}/restart`,{service});}async exportData(dataType,filters={}){return this.get(`${this.endpoint}/export/${dataType}`,filters);}async importData(dataType,file){return this.upload(`${this.endpoint}/import/${dataType}`,file);}}class APIServiceFactory{constructor(){this.camera=new CameraService();this.detection=new DetectionService();this.analytics=new AnalyticsService();this.system=new SystemService();}static handleError(error){console.error('API Error:',error);if(error instanceof APIError){switch(error.status){case 401:window.location.href='/login';break;case 403:window.dispatchEvent(new CustomEvent('showError',{detail:'You do not have permission to perform this action'}));break;case 404:window.dispatchEvent(new CustomEvent('showError',{detail:'The requested resource was not found'}));break;case 408:window.dispatchEvent(new CustomEvent('showError',{detail:'Request timed out. Please try again.'}));break;case 500:window.dispatchEvent(new CustomEvent('showError',{detail:'Server error. Please try again later.'}));break;default:window.dispatchEvent(new CustomEvent('showError',{detail:error.message || 'An unexpected error occurred'}));}}else{window.dispatchEvent(new CustomEvent('showError',{detail:'Network error. Please check your connection.'}));}return error;}}const api=new APIServiceFactory();export{APIServiceFactory,APIError,api};export default api;